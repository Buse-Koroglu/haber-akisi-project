import { Text, View } from "react-native";

const favorites = () => {
  return (
    <View className="flex-1 bg-surface items-center justify-center">
      <Text className="text-primary text-xl font-semibold">Favorites</Text>
    </View>
  );
};

export default favorites;
